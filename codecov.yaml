component_management:
  individual_components:
    - component_id: contract-blueprint
      name: Blueprint
      paths:
        - "contracts/blueprint/**"
    - component_id: contract-token
      name: BRLC Token
      paths:
        - "contracts/token/**"

comment:
  layout: "header, diff, flags, components"
